/**
 * Styles for Section.
 */

import colors from '../../../commons/colors'

const styles = {
  section: {
    minHeight: '100vh',
    backgroundColor: colors.lighterBlack,
    width: '100%',
  },
};

export default styles;