/**
 * Styles for Section.
 */

import colors from '../../../commons/colors'

const styles = {
  section: {
    minHeight: 400,
    backgroundColor: colors.lighterBlack,
    width: '100%',
  },
};

export default styles;