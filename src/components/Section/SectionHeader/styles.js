import colors from '../../../commons/colors'

const styles = {
  header: {
    padding: 30,
    paddingBottom: 20,
  },

  text: {
    color: colors.white,
    fontFamily: "'Noto Sans', 'Roboto', Tahoma, sans-serif",
    marginTop: 10,
    marginBottom: 20,
    fontSize: 32,
  }
}

export default styles